(function (app) {
  'use strict';

  app.registerModule('salaryAdvances');// The core module is required for special route handling; see /core/client/config/core.client.routes

}(ApplicationConfiguration));

